import{_ as a,c as o,o as t,ag as c}from"./chunks/framework.B-XtCDNB.js";const m=JSON.parse('{"title":"3.4. Semantic Analyzer","description":"3.4. Semantic Analyzer Vala.SemanticAnalyzer (vala/valasemanticanalyzer.vala) is a CodeVisitor that looks up the symbols that have been, parsed and checks if the symbols are being used correctly....","frontmatter":{"prev":{"text":"3.3. Symbol Resolution","link":"/guides/compiler-guide/03-00-the-vala-compiler/03-03-symbol-resolution"},"next":{"text":"3.5. Flow Analyzer","link":"/guides/compiler-guide/03-00-the-vala-compiler/03-05-flow-analyzer"}},"headers":[],"relativePath":"guides/compiler-guide/03-00-the-vala-compiler/03-04-semantic-analyzer.md","filePath":"guides/compiler-guide/03-00-the-vala-compiler/03-04-semantic-analyzer.md","lastUpdated":1786207193000}'),d={name:"guides/compiler-guide/03-00-the-vala-compiler/03-04-semantic-analyzer.md"};function l(r,e,i,n,s,h){return t(),o("div",null,[...e[0]||(e[0]=[c('<h1 id="_3-4-semantic-analyzer" tabindex="-1">3.4. Semantic Analyzer <a class="header-anchor" href="#_3-4-semantic-analyzer" aria-label="Permalink to &quot;3.4. Semantic Analyzer&quot;">​</a></h1><p><code>Vala.SemanticAnalyzer</code> (<code>vala/valasemanticanalyzer.vala</code>) is a <code>CodeVisitor</code> that looks up the symbols that have been, parsed and checks if the symbols are being used correctly.</p><p>This includes:</p><ul><li>Verifying that for each symbol type, the syntax is being followed</li><li>The rules of how each symbol can be used is being followed</li></ul><p>The key method to look at is <code>Vala.SemanticAnalyzer.analyze ()</code></p><h2 id="_3-4-1-how-the-built-in-symbol-types-are-looked-up" tabindex="-1">3.4.1 How The Built-in Symbol Types Are Looked Up <a class="header-anchor" href="#_3-4-1-how-the-built-in-symbol-types-are-looked-up" aria-label="Permalink to &quot;3.4.1 How The Built-in Symbol Types Are Looked Up&quot;">​</a></h2><p>Initially symbol types are looked up from the <code>Vala.CodeContext</code> (<code>vala/valacodecontext.vala</code>) <code>root</code> (<code>Vala.Namespace</code>, <code>vala/valanamespace.vala</code>).</p><p>In <code>Vala.Compiler</code> (<code>compiler/valacompiler.vala</code>) for the <code>run()</code> method, for each source filename given to the compiler, the <code>Vala.CodeContext.add_source_file_name ()</code> is called (unless the compiler is run with the Fast VAPIs feature enabled).</p><p>On each call to the <code>add_source_file_name ()</code> method on <code>Vala.CodeContext</code>, a default namespace <code>using</code> directive is added to the <code>Vala.SourceFile</code> (<code>vala/valasourcefile.vala</code>) object representing each source file.</p><p>The default namespace that gets added depends on the <code>profile</code> option set when the compiler is executed:</p><ul><li><code>gobject</code> (default): <code>using GLib;</code></li><li><code>posix</code>: <code>using Posix;</code></li></ul><p>This provides the implementation of the built-in types of the language.</p><p>The implementation of each type are then added to the namespace scope which is then looked up when the <code>Vala.SemanticAnalyzer.analyze()</code> method is called.</p><h2 id="_3-4-2-how-each-symbol-in-the-symbol-tree-is-checked" tabindex="-1">3.4.2 How Each Symbol in the Symbol Tree Is Checked <a class="header-anchor" href="#_3-4-2-how-each-symbol-in-the-symbol-tree-is-checked" aria-label="Permalink to &quot;3.4.2 How Each Symbol in the Symbol Tree Is Checked&quot;">​</a></h2><p>After looking up the built-in symbol types, in <code>Vala.SemanticAnalyzer.analyze ()</code>, the method then continues on by calling <code>Vala.CodeNode.check ()</code> (<code>vala/valacodenode.vala</code>) on the namespaces in the code context root.</p><p>After, <code>CodeContext.accept ()</code> is called with the semantic instance and the code visitor recursively goes through the entire symbol tree to ensure that all the symbols are being used correctly.</p><p>Also, <code>Vala.SemanticAnalyzer</code> contains methods and static functions that are used as shared reusable logic across the symbol tree of ensuring that symbol&#39;s are being used correctly. For example <code>Vala.SemanticAnalyzer.check_arguments ()</code> checks the arguments of an expression, ensuring that the correct amount of arguments are being used.</p><h2 id="_3-4-3-error-handling" tabindex="-1">3.4.3 Error Handling <a class="header-anchor" href="#_3-4-3-error-handling" aria-label="Permalink to &quot;3.4.3 Error Handling&quot;">​</a></h2><p>If while checking the symbols, any errors have been reported, after the semantic analyzer has finishing checking through the entire symbol tree, the compiler program will exit early instead of proceeding with the Flow Analyzer.</p><h2 id="_3-4-4-attributes" tabindex="-1">3.4.4. Attributes <a class="header-anchor" href="#_3-4-4-attributes" aria-label="Permalink to &quot;3.4.4. Attributes&quot;">​</a></h2><p>Semantic analysis is where most attribute-driven validation happens, since this is the stage that walks every symbol and calls <code>Vala.CodeNode.check ()</code> on it. There&#39;s no separate step for this - the same attribute accessors described in <a href="./03-02-parser.html#3-2-5-attributes">3.2.5. Attributes</a> are simply called from inside <code>check ()</code> and from computed properties that <code>check ()</code> relies on.</p><p>A few examples:</p><ul><li><code>Vala.Class.check ()</code> (<code>vala/valaclass.vala</code>) reads <code>has_attribute (&quot;Compact&quot;)</code> directly to reject a <code>[Compact]</code> class inheriting from a non-compact base class, reporting <code>Compact class `%s&#39; cannot inherit from non-compact class `%s&#39;</code>.</li><li><code>Vala.Class.is_compact</code> and the equivalent properties on <code>Vala.Struct</code> (<code>vala/valastruct.vala</code>) for <code>SimpleType</code>, <code>BooleanType</code>, <code>IntegerType</code> and <code>FloatingType</code> are backed by <code>has_attribute ()</code>, computed once and cached on first access.</li><li><code>Vala.SemanticAnalyzer</code> (<code>vala/valasemanticanalyzer.vala</code>) reaches into individual <code>CCode</code> arguments in its own helper methods while deciding how a symbol must be treated - for example <code>is_gobject_property ()</code> reads <code>array_length</code>, <code>array_null_terminated</code> and <code>delegate_target</code> to work out whether a property can be exposed as a GObject property, and <code>is_gobject_property_type ()</code> reads <code>has_type_id</code> to work out whether a struct type qualifies.</li></ul><h3 id="_3-4-4-1-version-and-deprecation-checks" tabindex="-1">3.4.4.1. Version and Deprecation Checks <a class="header-anchor" href="#_3-4-4-1-version-and-deprecation-checks" aria-label="Permalink to &quot;3.4.4.1. Version and Deprecation Checks&quot;">​</a></h3><p><code>[Version]</code> (and its deprecated predecessors <code>[Deprecated]</code>/<code>[Experimental]</code>) are checked by a dedicated helper class, <code>Vala.VersionAttribute</code> (<code>vala/valaversionattribute.vala</code>), rather than being read directly. Every <code>Vala.Symbol</code> lazily creates and caches one through its <code>version</code> property (<code>vala/valasymbol.vala</code>), and that object is what reads the <code>since</code>, <code>replacement</code>, <code>deprecated</code>, <code>deprecated_since</code>, <code>experimental</code> and <code>experimental_until</code> arguments.</p><p>Its <code>check ()</code> method is called wherever a symbol is <em>used</em>, rather than once for the whole file - from <code>Vala.MemberAccess</code> (<code>vala/valamemberaccess.vala</code>), <code>Vala.ObjectCreationExpression</code>, <code>Vala.Field</code>, <code>Vala.Parameter</code>, <code>Vala.LocalVariable</code> and <code>Vala.Class</code>. This is why a deprecation or experimental-API warning is reported once per call site or reference, not once globally for the declaration.</p>',26)])])}const u=a(d,[["render",l]]);export{m as __pageData,u as default};
