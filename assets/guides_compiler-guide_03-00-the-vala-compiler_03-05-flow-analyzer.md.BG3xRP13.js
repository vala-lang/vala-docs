import{_ as a,c as t,o,ag as r}from"./chunks/framework.B-XtCDNB.js";const p=JSON.parse('{"title":"3.5. Flow Analyzer","description":"3.5. Flow Analyzer Vala.FlowAnalyzer (vala/valaflowanalyzer.vala) is a CodeVisitor that checks that checks for the validity of the control flow logic of the source code. This is focused on whether...","frontmatter":{"prev":{"text":"3.4. Semantic Analyzer","link":"/guides/compiler-guide/03-00-the-vala-compiler/03-04-semantic-analyzer"},"next":{"text":"3.6. C Code Generation","link":"/guides/compiler-guide/03-00-the-vala-compiler/03-06-c-code-generation"}},"headers":[],"relativePath":"guides/compiler-guide/03-00-the-vala-compiler/03-05-flow-analyzer.md","filePath":"guides/compiler-guide/03-00-the-vala-compiler/03-05-flow-analyzer.md","lastUpdated":1786123399000}'),l={name:"guides/compiler-guide/03-00-the-vala-compiler/03-05-flow-analyzer.md"};function i(n,e,s,c,d,h){return o(),t("div",null,[...e[0]||(e[0]=[r('<h1 id="_3-5-flow-analyzer" tabindex="-1">3.5. Flow Analyzer <a class="header-anchor" href="#_3-5-flow-analyzer" aria-label="Permalink to &quot;3.5. Flow Analyzer&quot;">​</a></h1><p><code>Vala.FlowAnalyzer</code> (<code>vala/valaflowanalyzer.vala</code>) is a <code>CodeVisitor</code> that checks that checks for the validity of the control flow logic of the source code.</p><p>This is focused on whether the rules of the different blocks and control flow blocks (if-statements, switch statements etc.) are being followed correctly in the source code. As well as providing warnings about potential unexpected behaviour like a local variable being declared but never used in a block.</p><h2 id="_3-5-1-how-it-works" tabindex="-1">3.5.1 How It Works <a class="header-anchor" href="#_3-5-1-how-it-works" aria-label="Permalink to &quot;3.5.1 How It Works&quot;">​</a></h2><p>In <code>Vala.FlowAnalyzer.analyze ()</code>, the flow analyzer loops through each source file available in the <code>Vala.CodeContext</code> (<code>vala/valacodecontext.vala</code>) calls <code>Vala.SourceFile.accept ()</code> (<code>vala/valasourcefile.vala</code>) on each of them, recursively going through the symbol tree to builds a control flow graph.</p><p>Like in <code>Vala.Parser</code> there are <code>vist_</code> method overrides. These overrides contain the logic for checking the validity of the control flow related code between blocks, control flow blocks and related statements.</p><h2 id="_3-5-2-error-handling" tabindex="-1">3.5.2 Error Handling <a class="header-anchor" href="#_3-5-2-error-handling" aria-label="Permalink to &quot;3.5.2 Error Handling&quot;">​</a></h2><p>If any errors have been reported, after the flow analyzer has finishing building a control flow graph, the compiler program will exit early and not proceed to the next stage.</p><h2 id="_3-5-3-attributes" tabindex="-1">3.5.3. Attributes <a class="header-anchor" href="#_3-5-3-attributes" aria-label="Permalink to &quot;3.5.3. Attributes&quot;">​</a></h2><p><code>Vala.FlowAnalyzer</code> (<code>vala/valaflowanalyzer.vala</code>) reads a couple of attributes directly, the same way described in <a href="./03-02-parser.html#3-2-5-attributes">3.2.5. Attributes</a>:</p><ul><li><code>visit_method ()</code> skips its &quot;Method <code>%s&#39; never used&quot; warning for an internal method on a type with </code>[DBus]<code>when</code>get_attribute_bool (&quot;DBus&quot;, &quot;visible&quot;, true)` is true, since a visible D-Bus method can be called externally even though nothing in the Vala code calls it.</li><li><code>NoReturn</code> is checked when analyzing a method call statement: if the method being called has <code>has_attribute (&quot;NoReturn&quot;)</code>, everything after that call is marked unreachable, the same way it would be after a <code>return</code> or <code>throw</code>.</li></ul><h3 id="_3-5-3-1-warning-about-unused-attributes" tabindex="-1">3.5.3.1. Warning About Unused Attributes <a class="header-anchor" href="#_3-5-3-1-warning-about-unused-attributes" aria-label="Permalink to &quot;3.5.3.1. Warning About Unused Attributes&quot;">​</a></h3><p>Because nothing interprets attributes centrally, an attribute that no stage of the compiler ever reads would otherwise be silently ignored - including one whose name is simply misspelled. <code>Vala.UsedAttr</code> (<code>vala/valausedattr.vala</code>) exists to catch that case. It&#39;s the one code visitor left in the compiler that is dedicated to attributes, though it only checks that attribute names (and their arguments) are recognized - it doesn&#39;t interpret what they mean.</p><p>It holds a <code>marked</code> map of every attribute name valac knows, along with the argument names each one accepts, populated from a <code>valac_default_attrs</code> list in the same file (see <a href="./03-02-parser.html#3-2-5-3-attributes-recognized-by-vala">3.2.5.3. Attributes Recognized by Vala</a> for what that list contains). <code>Vala.CodeContext</code> (<code>vala/valacodecontext.vala</code>) constructs one as its <code>used_attr</code> property, and <code>Vala.CodeContext.check ()</code> calls <code>check_unused ()</code> on it as the final step - after the semantic analyzer and the flow analyzer have both run, and only if neither reported errors. The visitor then walks the symbol tree and, for every attribute it finds that isn&#39;t in the map, reports:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Attribute `Foo&#39; never used</span></span></code></pre></div><p>with the same check applied to individual arguments of a recognized attribute. This is a warning rather than an error, so an unknown attribute does not stop compilation.</p>',16)])])}const b=a(l,[["render",i]]);export{p as __pageData,b as default};
