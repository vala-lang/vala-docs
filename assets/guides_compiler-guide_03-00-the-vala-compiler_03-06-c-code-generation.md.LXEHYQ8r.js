import{_ as o,c as a,o as t,ag as c}from"./chunks/framework.B-XtCDNB.js";const d="/assets/vala-gdbus-server-module.WWX3MnHg.png",m=JSON.parse('{"title":"3.6. C Code Generation","description":"3.6. C Code Generation In the previous Vala compiler steps, we verified that the validity of the Vala code. Now the compiler is ready to generate C code from the Vala source code. In the...","frontmatter":{"prev":{"text":"3.5. Flow Analyzer","link":"/guides/compiler-guide/03-00-the-vala-compiler/03-05-flow-analyzer"},"next":{"text":"3.7. C Code Compilation and Linking","link":"/guides/compiler-guide/03-00-the-vala-compiler/03-07-c-code-compilation-and-linking"}},"headers":[],"relativePath":"guides/compiler-guide/03-00-the-vala-compiler/03-06-c-code-generation.md","filePath":"guides/compiler-guide/03-00-the-vala-compiler/03-06-c-code-generation.md","lastUpdated":1786123399000}'),r={name:"guides/compiler-guide/03-00-the-vala-compiler/03-06-c-code-generation.md"};function i(n,e,s,l,h,u){return t(),a("div",null,[...e[0]||(e[0]=[c('<h1 id="_3-6-c-code-generation" tabindex="-1">3.6. C Code Generation <a class="header-anchor" href="#_3-6-c-code-generation" aria-label="Permalink to &quot;3.6. C Code Generation&quot;">​</a></h1><p>In the previous Vala compiler steps, we verified that the validity of the Vala code. Now the compiler is ready to generate C code from the Vala source code.</p><p>In the <code>Vala.Compiler.run ()</code> (<code>compiler/valacompiler.vala</code>) method, the compiler will eventually call <code>Vala.CodeContext.codegen.emit ()</code> (<code>vala/valacodecontext.vala</code>) which starts the C code generation process.</p><h2 id="_3-6-1-how-the-code-generation-works" tabindex="-1">3.6.1 How The Code Generation Works <a class="header-anchor" href="#_3-6-1-how-the-code-generation-works" aria-label="Permalink to &quot;3.6.1 How The Code Generation Works&quot;">​</a></h2><h3 id="_3-6-1-1-deciding-which-vala-codegenerator-instance-type-to-use" tabindex="-1">3.6.1.1 Deciding which Vala.CodeGenerator instance type to use <a class="header-anchor" href="#_3-6-1-1-deciding-which-vala-codegenerator-instance-type-to-use" aria-label="Permalink to &quot;3.6.1.1 Deciding which Vala.CodeGenerator instance type to use&quot;">​</a></h3><p>Vala.CodeContext.codegen is a property for an instance of <code>Vala.CodeGenerator</code> (<code>vala/valacodegenerator.vala</code>). The object type is different based on the compiler profile that the user set:</p><ul><li>GObject profile: <code>GDBusServerModule</code> (<code>codegen/valagdbusservermodule.vala</code>).</li><li>Otherwise: <code>CCodeDelegateModule</code> (<code>codegen/valaccodedelegatemodule.vala</code>)</li></ul><h3 id="_3-6-1-2-modules-pattern-with-c-code-generator" tabindex="-1">3.6.1.2 Modules Pattern with C Code Generator <a class="header-anchor" href="#_3-6-1-2-modules-pattern-with-c-code-generator" aria-label="Permalink to &quot;3.6.1.2 Modules Pattern with C Code Generator&quot;">​</a></h3><p><code>Vala.CodeGenerator</code> is an abstract class that is a code visitor for generating code. This is the base class for code generators in the compiler, it contains abstract methods to be overriden by subclasses that help wit hthe code generation process too.</p><p>The actual code generation logic is contained in its subclasses. The direct subclass of <code>Vala.CodeGenerator</code> is <code>Vala.CCodeBaseModule</code> (<code>codegen/valaccodebasemodule.vala</code>). Which actually overrides the <code>Vala.CodeVisitor</code> methods.</p><p><code>Vala.CCodeBaseModule</code> contains the logic that provides the base functionality for generating C code from Vala code.</p><p>Inheritance is used to stack code generation functionality between subclasses, expanding on the method overrides of parent classes. are modules that provide functionality for the code generation process.</p><p><code>Vala.CCodeBaseModule</code> is a module that provides base functionality for the code generation process. For additional functionality, <code>Vala.CCodeStructModule</code> (<code>codegen/valaccodestructmodule.vala</code>) extends the <code>Vala.CCodeBaseModule</code> class, however the module specifically handles how C structs should be generated from Vala code. We keep stacking functionality using modules in the inheritance tree until we&#39;ve reached the desired amount of functionality. That&#39;s where the instance types based on the profiles come in. They&#39;ve implemented an appropriate amount of functionality to be able to generate code for those profiles.</p><div class="info custom-block"><p class="custom-block-title">Note</p><p>All of the code generation modules (e.g. <code>CCodeArrayModule</code>, <code>CCodeAssignmentModule</code>, <code>GAsyncModule</code>, <code>GObjectModule</code>, and others) live in the <code>codegen</code> directory, following the <code>vala&lt;class-name-lowercase&gt;.vala</code> naming convention.</p></div><p>However, as more features are added to the language, the inhertiance trees for the code generation classes <code>Vala.CodeGenerator</code> classes may grow further. Or a slightly different inheritance tree may be created for new profile, with modules that are specific for that new profile.</p><p>Here&#39;s the object hiearchy graph for the <code>Vala.GDBusServerModule</code> class:</p><p><img src="'+d+'" alt="Vala.GDBusServerModule Object Hierarchy Graph"></p><h2 id="_3-6-2-attributes" tabindex="-1">3.6.2. Attributes <a class="header-anchor" href="#_3-6-2-attributes" aria-label="Permalink to &quot;3.6.2. Attributes&quot;">​</a></h2><p>C code generation is the largest single consumer of attributes in the compiler, specifically of <code>CCode</code> - see <a href="./03-02-parser.html#3-2-5-attributes">3.2.5. Attributes</a> for how attributes are parsed and stored in general.</p><p>Wherever a code generation module needs to know how a Vala symbol maps onto C - its C identifier, header filename, type id, ref and unref functions and so on - it calls one of the <code>Vala.get_ccode_*()</code> functions in <code>codegen/valaccode.vala</code>, such as <code>Vala.get_ccode_name ()</code>, <code>Vala.get_ccode_const_name ()</code>, <code>Vala.get_ccode_type_name ()</code> and <code>Vala.get_ccode_header_filenames ()</code>. There are around seventy of these public wrapper functions in total.</p><p>Each one is backed by <code>Vala.CCodeAttribute</code> (<code>codegen/valaccodeattribute.vala</code>), which wraps a single code node and exposes the C-level information the generator needs as properties - <code>name</code>, <code>const_name</code>, <code>type_name</code>, <code>header_filenames</code>, <code>prefix</code>, <code>ref_function</code> and many more. Every property lazily reads the corresponding <code>CCode</code> argument from the node&#39;s raw <code>Vala.Attribute</code>, and, when that argument wasn&#39;t given in the source, computes a sensible default from the Vala symbol instead. So <code>[CCode (cname = &quot;...&quot;)]</code> overrides a C name that would otherwise be derived automatically from the Vala declaration&#39;s name.</p><p><code>Vala.CodeNode</code> supports caching this generically: a node carries an <code>attributes_cache</code> array of <code>Vala.AttributeCache</code> slots, accessed with <code>get_attribute_cache ()</code> and <code>set_attribute_cache ()</code>, and each kind of cache claims a unique slot index from the static <code>Vala.CodeNode.get_attribute_cache_index ()</code>. <code>Vala.CCodeAttribute</code> extends <code>Vala.AttributeCache</code>, so exactly one instance is created per code node - the first time any <code>get_ccode_*</code> function is called on it - and every later call reuses it rather than re-reading and re-deriving attribute strings.</p>',22)])])}const g=o(r,[["render",i]]);export{m as __pageData,g as default};
